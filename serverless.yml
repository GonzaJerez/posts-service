service: posts-service

provider:
  name: aws
  region: sa-east-1
  stage: prod
  httpApi:
    id: ${env:API_GATEWAY_ID}

useDotenv: true

package:
  patterns:
    - '!**/**'
    - dist/**

functions:
  main:
    handler: dist/main.handler
    name: posts-service
    runtime: nodejs20.x
    timeout: 15
    events:
      - httpApi:
          path: '/posts'
          method: ANY
      - httpApi:
          path: '/posts/{proxy+}'
          method: ANY
      - sqs: ${env:SQS_QUEUE_ARN}
    environment:
      NODE_ENV: prod
      SERVER_MODE: serverless
      MONGO_URI: ${env:MONGO_URI}
      AWS_BUCKET_NAME: ${env:AWS_BUCKET_NAME}
      AWS_BUCKET_REGION: ${env:AWS_BUCKET_REGION}
      AUTHORS_QUEUE_URL: ${env:AUTHORS_QUEUE_URL}

plugins:
  - serverless-offline
